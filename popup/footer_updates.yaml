action: fire-dom-event
browser_mod:
  service: browser_mod.popup
  data:
    title: Aktualizacje
    # `size:` was the pre-2.6 spelling; it now maps onto `initial_style:`, whose value is
    # a *popup style* name. Popup styles have no global config (browser_mod has no YAML
    # config and nothing named `popup_styles` in its backend - the frontend just does
    # `this._popupStyles = <popup parameter>`), so they are declared here, per popup.
    # The theme sets `popup-min-width` for every `normal` popup, hence the override:
    # browser_mod's unconditional `ha-dialog, ha-adaptive-dialog` rule reads
    # `--popup-min-width` / `--popup-max-width` / `--popup-width-full`.
    # NOTE: a style's `styles` is injected as `:host([narrow]) { ... }`, so it must be
    # declarations only - selectors would need browser CSS nesting.
    initial_style: narrow
    # Default sequence is `wide` -> `normal`; without this the first title tap would blow
    # the popup back up to the theme's ~92vw. Tap once to widen the release notes.
    style_sequence:
      - narrow
      - wide
    popup_styles:
      - style: narrow
        include_styles:
          - normal
        styles: |
          --popup-min-width: min(92vw, 560px);
          --popup-max-width: min(92vw, 560px);
          --popup-width-full: min(92vw, 560px);
    content:
      type: grid
      columns: 1
      square: false
      cards:
        - type: custom:button-card
          entity: sensor.current_version
          template: updates_hass
          variables:
            available: sensor.template_updates
            latest: sensor.hass_version_latest
            latest_beta: sensor.hass_version_latest_beta
            release_notes: sensor.hass_release_notes
            release_notes_beta: sensor.hass_release_notes_beta

        - type: custom:button-card
          entity: sensor.current_version
          name: Aktualizuj HASS
          icon: mdi:update
          variables:
            latest: sensor.hass_version_latest
            latest_beta: sensor.hass_version_latest_beta
          # styles:
          #   card:
          #     - width: max-content
          #     - filter: >
          #         [[[
          #           return `[[[
          #             if (entity) {
          #               return entity.state === states['sensor.hass_version_latest'].state ||
          #                 entity.state === states['sensor.hass_version_latest_beta'].state
          #                   ? 'opacity(30%)'
          #                   : 'drop-shadow(3px 3px 5px #00000050)';
          #             }
          #           ]]]`
          #         ]]]
          #     - display: >
          #         [[[
          #           return `[[[
          #             return entity
          #               ? 'flex'
          #               : 'none';
          #           ]]]`
          #         ]]]
          tap_action:
            action: perform-action
            perform_action: browser_mod.sequence
            data:
              sequence:
                - service: browser_mod.notification
                  data:
                    message: Starting update...
                - service: script.turn_on
                  data:
                    entity_id: script.homeassistant_core_update
          template: updates_hass_icon_name

        ### HACS
        - type: custom:button-card
          variables:
            updates: sensor.template_updates
            supervisor_updates: sensor.supervisor_updates
            hacs_installed: sensor.hacs_installed
          template: updates

        - type: custom:button-card
          entity: sensor.hacs
          name: Otwórz HACS
          # `template` entries are resolved literally, so the js template that used
          # to sit here always threw "Button-card template '...' is missing!" and
          # this card rendered as an error card. The platform switch now lives in
          # the action (`hacs_iframe` / `hacs_navigate_ios` are kept for reference).
          template: updates_hacs_icon_name
          tap_action: |
            [[[
              // `hass.panels.hacs` only exists for administrators, so on a restricted
              // session (e.g. the non-admin "Tablet" kiosk user) this threw a TypeError
              // on tap and the row did nothing. Fall back to a no-op action.
              const url_path = hass.panels?.hacs?.url_path;
              if (!url_path) {
                return { action: 'none' };
              }
              const url = `/${url_path}`;
              return window.navigator.userAgent.match(/iPhone/i)
                ? {
                    action: 'fire-dom-event',
                    browser_mod: {
                      service: 'browser_mod.javascript',
                      data: {
                        code: `window.location.href = '${url}';`
                      }
                    }
                  }
                : {
                    action: 'fire-dom-event',
                    browser_mod: {
                      service: 'browser_mod.popup',
                      data: {
                        title: 'HACS',
                        size: 'fullscreen',
                        content: {
                          type: 'iframe',
                          aspect_ratio: '16x9.15',
                          url
                        }
                      }
                    }
                  };
            ]]]

        ### supervisor
        - type: custom:button-card
          entity: sensor.supervisor_updates
          template: updates_supervisor

        - type: custom:button-card
          entity: sensor.supervisor_updates
          name: Otwórz Supervisor
          icon: mdi:open-in-new
          styles:
            card:
              - width: max-content
              - filter: >
                  [[[
                    return states['binary_sensor.template_supervisor_update']?.state === 'off' ?
                      'opacity(30%)' :
                      'drop-shadow(3px 3px 5px #00000050)';
                  ]]]
          template: supervisor_iframe
